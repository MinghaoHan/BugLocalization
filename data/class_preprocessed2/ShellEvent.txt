org eclips wt event org eclips wt widget event shell event type event do it serial version id shell event event do it do it string to str string string to str string substr string length remov trail do it do it